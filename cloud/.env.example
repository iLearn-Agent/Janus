# The public deployment uses separate environment files for each service.
# Start with deploy/env/compose.env.example, api.env.example, worker.env.example,
# migrator.env.example, storage.env.example and release.env.example.
# compose.env contains shared non-secret Docker names only. Do not combine the
# service environments or secrets into one production .env file.
