-- Proposal tables were consolidated into 008_evolution_authority.sql before release.
-- Keep this migration as an immutable compatibility marker for environments that
-- discovered the intermediate filename.
SELECT 1;
