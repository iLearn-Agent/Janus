# Include this location inside the public Janus server block.
location /janus/test_releases/ {
    alias /path/to/janus-cloud/test_releases/;
    autoindex off;
    add_header Cache-Control "no-cache";
}
